<Window x:Class="QueryEditor1C.FindReplaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Поиск и замена" Height="250" Width="450"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Поиск -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Найти:" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtFind" Margin="0,0,0,5"/>

        <!-- Замена -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Заменить на:" VerticalAlignment="Center" Margin="0,0,10,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtReplace" Margin="0,0,0,5"/>

        <!-- Опции -->
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10">
            <CheckBox x:Name="chkCaseSensitive" Content="Учитывать регистр" Margin="0,0,20,0"/>
            <CheckBox x:Name="chkWholeWord" Content="Слово целиком"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnFindNext" Content="Найти далее" Width="100" Margin="0,0,5,0" Click="FindNext_Click"/>
            <Button x:Name="btnReplace" Content="Заменить" Width="100" Margin="0,0,5,0" Click="Replace_Click"/>
            <Button x:Name="btnReplaceAll" Content="Заменить все" Width="100" Margin="0,0,5,0" Click="ReplaceAll_Click"/>
            <Button Content="Закрыть" Width="80" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
